import React from "react";

function Navbar() {
    return (
        <section className="nav-container">
            <nav>
                <a href="#">RESUME </a>
                <a href="#">SKILLS</a>
                <a href="#">PROJECTS</a>
                <a href="#">CONTACT</a>
            </nav>

        </section>

    )
}

export default Navbar